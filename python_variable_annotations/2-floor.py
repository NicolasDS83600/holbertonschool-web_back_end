#!/usr/bin/env python3
"""Provide a function to compute the floor of a number."""

import math


def floor(n: float) -> int:
    """Return the largest integer less than or equal to n."""
    return math.floor(n)
